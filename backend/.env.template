# -------------------------------
# Configurações do Banco de Dados
# -------------------------------

# URI de conexão com o MongoDB (inclui usuário, senha e cluster)
MONGODB_URI=mongodb+srv://<usuario>:<senha>@<cluster>.mongodb.net

# Nome do banco de dados (não é o nome da coleção)
DB_NAME=nome-do-banco

# -------------------------------
# Configurações do Servidor
# -------------------------------

# Porta em que o servidor Express irá rodar
PORT=3000

# Ambiente da aplicação: development | production | test
NODE_ENV=production

# -------------------------------
# Segurança e Autenticação
# -------------------------------

# Chave secreta para assinar tokens JWT
JWT_SECRET=secret-key

# -------------------------------
# Configurações de CORS
# -------------------------------

# Lista de origens permitidas (separadas por vírgula)
CORS_ORIGIN=http://localhost:3001,https://meusite.com

# -------------------------------
# Rate Limiting
# -------------------------------

# Número máximo de requisições permitidas por IP em um intervalo
RATE_LIMIT_MAX=100

# -------------------------------
# Helmet
# -------------------------------

# URLs permitidas para CSP (Content Security Policy)
CSP_CONNECT_SRC=http://localhost:3000,https://minhaapi.com
